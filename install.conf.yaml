---
- defaults:
    link:
      create: true
- link:
    ~/.claude/settings.json: .claude/settings.json
    ~/.config/mise: .config/mise
    ~/.config/pnpm: .config/pnpm
    ~/.config/uv: .config/uv
    ~/.npmrc: .npmrc
    ~/.tmux.conf: .tmux.conf
    ~/.vimrc: .vimrc
